<?xml version="1.0" encoding="utf-8" ?>

<!-- RIPPLE EFFECT FOR BORDERLESS BUTTON -->

<ripple xmlns:android="http://schemas.android.com/apk/res/android"
    android:color="?android:colorControlHighlight" >
    <item android:id="@android:id/mask" >
        <shape xmlns:android="http://schemas.android.com/apk/res/android"
            android:shape="rectangle" >
            <corners android:radius="2dp" />
            <solid android:color="@color/white" />
            <!-- This color will NOT get applied - because in <item>
                 android:id="@android:id/mask" is set, the color will ALWAYS
                 be transparent. However, the solid color value is still
                 required for ripple affect to display.
                 
                 To change the color of the button, remove @android:id/mask -->
        </shape>
    </item>
</ripple>