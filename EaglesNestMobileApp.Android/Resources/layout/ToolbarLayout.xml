<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:id="@+id/toolbar"
        android:layout_width="match_parent"
        android:layout_height="?attr/actionBarSize"
        android:background="?attr/modToolbarColor"
        android:theme="?attr/modToolbarTheme"
        app:popupTheme="?attr/modToolbarPopupTheme"
        app:layout_scrollFlags="scroll|enterAlways|snap"
        >
</android.support.v7.widget.Toolbar>
        <!-- app:layout_scrollFlags="scroll|enterAlways" 
             Definition: The view becomes visible when scrolling up.
             Exposes the toolbar as soon as scrolling up takes place. -->
        <!-- app:layout_scrollFlags="scroll|enterAlways|snap"
             The toolbar will snap to its closest side when scrolled past
             or below 50% of its height -->
        <!-- popupTheme is used when inflating popup menus inside the toolbar -->

        